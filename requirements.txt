# Core
httpx>=0.27
rich>=13.7
prompt_toolkit>=3.0
pyyaml>=6.0
psutil>=5.9

# Web UI
fastapi>=0.111
uvicorn[standard]>=0.30
websockets>=12.0
python-multipart>=0.0.9

# Memory / RAG
chromadb>=0.5
sentence-transformers>=3.0

# Job scheduler / versioning (stdlib sqlite3 — no extra dep)

# Multi-agent (uses same httpx/Ollama client — no extra dep)

# Voice
openai-whisper>=20231117
sounddevice>=0.4.6
soundfile>=0.12
pyttsx3>=2.90

# Remote compute
paramiko>=3.4

# Containers (uses subprocess — no extra dep)

# Reports
reportlab>=4.2
weasyprint>=62.0

# Visualization
matplotlib>=3.9
plotly>=5.22
kaleido>=0.2

# Scientific (optional but recommended)
numpy>=1.26
scipy>=1.13
pandas>=2.2

